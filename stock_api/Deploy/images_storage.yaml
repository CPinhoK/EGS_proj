apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tista-images-pvc
  namespace: tista
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Mi